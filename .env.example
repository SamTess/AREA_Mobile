ANDROID_HOME=$PWD/Android/Sdk
PATH=$PATH:$ANDROID_HOME/emulator
PATH=$PATH:$ANDROID_HOME/platform-tools

# ======================
# MOCK CONFIGURATION
# ======================

# Enable mock mode for authentication (true/false)
# Set to false when you have a real backend ready
EXPO_PUBLIC_USE_MOCK=true

# Mock API delay in milliseconds
# Simulates network latency (0 for instant response)
EXPO_PUBLIC_MOCK_DELAY=1000

# ======================
# API CONFIGURATION
# ======================

# Backend API URL(s)
# Used when EXPO_PUBLIC_USE_MOCK=false
# Android emulator: http://10.0.2.2:8080
# iOS simulator: http://localhost:8080 (si le backend tourne sur ta machine)
# iOS appareil réel: utilise l'IP LAN de ta machine (ex: http://192.168.1.42:8080)
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080
# Optional override only for iOS
# Pour simulateur: http://localhost:8080
# Pour appareil réel: http://192.168.1.42:8080
EXPO_PUBLIC_API_URL_IOS=http://localhost:8080

# Web base URL (if needed by backend for forming redirects)
# Should match backend OAUTH_REDIRECT_BASE_URL (web), par exemple
# - Dev simulateur iOS: http://localhost:3000
# - Dev appareil réel:  http://192.168.1.42:3000
EXPO_PUBLIC_WEB_BASE_URL=http://localhost:3000

# ======================
# NOTES
# ======================
# - Never commit .env file to git
# - Always use EXPO_PUBLIC_ prefix for Expo environment variables
# - Change USE_MOCK to false before production deployment
# - After changing environment variables, rebuild the app:
#   yarn run
#
# Backend API Requirements (when USE_MOCK=false):
# - Auth via HttpOnly cookies; always send credentials: include
# - POST /api/auth/login | POST /api/auth/register | POST /api/auth/logout
# - GET  /api/auth/me (returns { message, user })
# - PUT  /api/users/{id} for profile update (if available)
