import { Dimensions } from 'react-native';

export const { width: screenWidth, height: screenHeight } = Dimensions.get('window');

export const CARD_WIDTH = 160;
export const CARD_HEIGHT = 100;
export const REMOVE_ZONE_HEIGHT = 100;
